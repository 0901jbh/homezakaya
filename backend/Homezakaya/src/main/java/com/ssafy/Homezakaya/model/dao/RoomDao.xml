<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.Homezakaya.model.dao.RoomDao">
    <insert id="insertRoom" parameterType="RoomDto" >
        insert into room(roomId, title, password, category, hostId, createdTime, personLimit, personCount) values(#{roomId}, #{title}, #{password}, #{category}, #{hostId}, #{createdTime}, #{personLimit}, #{personCount})
    </insert>

    <select id="selectRooms" resultType="RoomDto">
        select * from room
    </select>

    <select id="checkPassword" parameterType="RoomDto">
        select * from room where roomId = #{roomId} and password = #{password}
    </select>

    <update id="enterRoom" parameterType="Integer">
        update room set personCount = personCount + 1 where roomId = #{roomId}
    </update>

    <update id="quitRoom" parameterType="Integer">
        update room set personCount = personCount - 1 where roomId = #{roomId}
    </update>

    <delete id="deleteRoom" parameterType="Integer">
        delete from post where roomId = #{no}
    </delete>

</mapper>